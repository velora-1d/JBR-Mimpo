import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF10B981);
  static const Color primaryDark = Color(0xFF059669);
  static const Color primaryLight = Color(0xFF34D399);
  static const Color deep = Color(0xFF065F46);

  // Background colors
  static const Color bgLight = Color(0xFFF8FAFB);
  static const Color bgDark = Color(0xFF111827);

  // Splash Screen Gradients
  static const Color splashGradStart = Color(0xFF00422B);
  static const Color splashGradMid = Color(0xFF002113);
  static const Color splashGradEnd = Color(0xFF001A0D);

  // Text colors
  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textWhite = Colors.white;

  // Functional colors
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
  static const Color success = Color(0xFF10B981);

  // Surface colors from Design
  static const Color surfaceContainer = Color(0xFFEAEDFF);
  static const Color primaryFixedDim = Color(0xFF4EDEA3);
}
